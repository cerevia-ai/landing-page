// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true, // faster builds using SWC

  // If you plan to use external images (e.g., logos or Streamlit thumbnails)
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**', // or specify exact domains
      },
    ],
  },

  // Optional: Base path if deploying under a subfolder
  // basePath: '/your-subfolder',

  // Optional: Redirect Streamlit links through your domain (if needed)
  async redirects() {
    return [
      {
        source: '/cardio',
        destination: 'https://your-cardiology-streamlit-app.streamlit.app',
        permanent: true,
      },
      {
        source: '/alzheimer',
        destination: 'https://your-alzheimer-streamlit-app.streamlit.app',
        permanent: true,
      },
    ];
  },
};

module.exports = nextConfig;
